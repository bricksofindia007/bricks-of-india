# BOI 360° Evidence Audit — 2026-09-19

Read-only diagnostic audit, run per `docs/AUDIT_RUNBOOK.md`'s standing monthly procedure (requested explicitly this session even though most tracker items are already resolved — its findings are authoritative over this session's own prior manual checks if anything conflicts). Every finding carries an evidence tag: **[RUN-LOG]**, **[GH-API]**, **[DB-QUERY]** (Supabase, project `hqpaiarhmiocmjrzjhtw`), **[LIVE-FETCH]** (curl against `https://bricksofindia.com`), **[REPO-GREP]**. Tracker prose is treated as a claim under test, never as evidence on its own.

**Headline: no conflicts found.** Every independently-verified finding below corroborates what `BOI_MASTER_TRACKER.md`'s own 2026-09-17/09-18 entries already documented in detail. The one genuinely new item this audit surfaced — the recurring Meta `(#10)` permission error now confirmed in a second, independent pipeline — is filed as issue #141 below.

---

## Part A — Cron truth table

39 workflow files in `.github/workflows/` (up from 18 at the 2026-07-04 audit). [REPO-GREP] cron lines + [GH-API] `gh run list --workflow=<file> --limit 3`, run fresh this session.

**Healthy, on-schedule (24 of 39):** brief, content-quality, credit-budget-check, health-check, ig-token-refresh, model-canary, generate-drafts, generate-guide-weekly, guide-staleness-monthly, monthly-audit-reminder, publish-drafts, radar, reviews-weekly-refresh, scrape-prices, snapshot-prices, video-generate-daily (in-progress at audit time, prior run success), video-generate-quiet-panic, video-publish-poller, video-publish-poller-quiet-panic, video-rework-poller-quiet-panic, ci, deploy, deploy-cloudflare, lint-workflows — all last-3-runs green or with an isolated, already-explained failure.

**Failing, 3/3 recent runs (confirmed real, not flaky):**
- **catalogue-audit.yml** — fails on real content-completeness data (48/1,159 listed sets missing `lego_mrp_inr`), not a crash. **Already tracked: issue #17**, and the tracker's 2026-09-17T19:41Z entry independently confirms the same number. No drift — consistent with tracker.
- **technical-hygiene.yml** — fails on `ReviewRouting: FAIL: 1 slug(s) exist in both reviews and news_articles: lego-the-endurance-10335-worth-22899`. **Already tracked: issue #128** (open). No drift.
- **code-audit.yml** — fails on `npm audit --audit-level=high`: 9 vulnerabilities (2 moderate, 7 high), including `sharp`/libheif (GHSA-rgj7-g3m4-5g8c). **Already tracked: PR #41**, "still OPEN, still the 9-vulnerability npm-audit blocker, untouched" per the tracker's own 2026-09-18 re-check. No drift.

**One-off failures, isolated to a real, dated infra incident (09-13 07:08 UTC – 09-14 09:14 UTC):** `sync-catalogue.yml` (09-13), `retiring-soon.yml` (09-13), `social-automation.yml` (09-13/09-14), `populate-mrp.yml` (09-14), `mrp-refresh.yml` (09-14), `video-rejection-reminder.yml` (09-14) all failed with the identical `Service for this project is restricted due to the following violations: exceed_storage_size_quota` message [RUN-LOG, runs `34745328907`, `34744496994`, `34756410776`, `34819648140`, `34820465492`]. **Already documented in full in the tracker's "Storage-quota incident investigation" (2026-09-17) entry.** Independently re-confirmed this audit:
  - [mcp-Supabase] Project `hqpaiarhmiocmjrzjhtw` status is currently `ACTIVE_HEALTHY`.
  - [DB-QUERY] Current storage-bucket total: `video-master-assets` 64MB, `social-assets` 333MB, `quiet-panic-assets` 110MB — **507MB total**, well under quota.
  - All of these same workflows' successors (daily crons: scrape-prices, snapshot-prices, generate-drafts, content-quality) ran green every day 09-16 through 09-19.
  - **Caveat, stated plainly, not glossed over:** `sync-catalogue.yml`, `retiring-soon.yml`, `mrp-refresh.yml`, `populate-mrp.yml`, `video-rejection-reminder.yml` are weekly crons — none has fired again since the incident (next fire 09-21 for the Monday set, 09-20 for the Sunday set), so "resolved" here is inferred from the healthy project status and other workflows' recovery, not from a direct same-workflow re-run. Flagged as such, not asserted as directly re-proven.

**Never fired (expected, not a gap):**
- `lint-tracker-dashboard-sync.yml` — scoped to `pull_request` on `BOI_MASTER_TRACKER.md`/`admin/dashboard.html` paths only [REPO-GREP]. No PR has touched either file since it was added (2026-08-23); all tracker changes go via the direct-push docs-only fast path CLAUDE.md explicitly carves out. Working as designed.
- `retention-cleanup.yml` — weekly Sunday 05:00 UTC, merged 2026-09-17 (Thursday). First possible fire is 2026-09-20 — hasn't happened yet, correctly.
- `video-pending-approval-digest.yml` — daily 03:25 UTC, merged 2026-09-19 04:31:40 UTC (today, after today's fire time). First real opportunity is 2026-09-20 03:25 UTC.
- `video-script-gen-test-quiet-panic.yml`, `video-feasibility-test.yml`, `video-publish-manual.yml`, `youtube-backfill.yml` — manual-dispatch-only by design, per existing tracker precedent.

**Proven only by manual dispatch, cron not yet exercised (per the runbook's explicit "successful run ≠ automation works unattended" rule):**
- **video-retry-missing-platform.yml** (hourly, `45 * * * *`) — merged 04:31:40 UTC today; two `workflow_dispatch` runs exist (04:32 success, 04:39 failure — see below), no `schedule`-triggered run yet as of this audit (05:0x UTC), even though one `:45` window has passed. Too early to call this a gap (GH Actions schedule triggers commonly lag 5–15 min after a workflow file first lands); flagged to re-check next audit, not asserted broken.

---

## Part B — Generation pipeline health

[DB-QUERY] `generator_runs`, last 10 rows. Schema has evolved since July's audit — `drafts_deferred`/`drafts_routed_to_review`/`drafts_failed` are all 0 across every sampled run now; the split is entirely `drafts_succeeded` vs `drafts_lint_failed`.

| started_at (UTC) | attempted | succeeded | lint_failed |
|---|---|---|---|
| 09-18 13:03 | 30 | 4 | 26 |
| 09-17 13:39 | 30 | 1 | 29 |
| 09-16 13:37 | 30 | 3 | 27 |
| 09-15 13:43 | 29 | 4 | 25 |
| 09-12 12:15 | 24 | 4 | 20 |
| 09-11 12:55 | 20 | 1 | 19 |
| 09-10 12:59 | 24 | 2 | 22 |
| 09-09 13:03 | 30 | 8 | 22 |
| 09-08 12:57 | 20 | 4 | 16 |
| 09-07 14:21 | 28 | 11 | 17 |

**No stall.** Every one of the 10 sampled runs produced 1–11 successes; no trend toward zero. This is a materially different (and healthier) shape than the July 2026 stall (which was 100%-deferred due to a truncated Gemini error) — here the dominant outcome is `lint_failed` (a strict quality gate correctly rejecting most drafts), and `news_articles` published 1–11 rows/day on every sampled day 09-15 through 09-18 [DB-QUERY].

**`pending_drafts` approved-queue check — the exact leading indicator July's stall was caught on, re-run fresh:** [DB-QUERY] 28/28 `approved` rows currently have `draft_body IS NULL`. **This is not a stall recurrence** — all 28 rows share one identical `created_at` timestamp, `2026-09-18 19:53:47.998571+00`, i.e. a single fresh RADAR-03 batch that landed *after* today's already-completed `generate-drafts.yml` run (09-18 13:03) and *before* the next scheduled one (~09-19 13:00) — a normal one-cycle wait, not an aged backlog. Confirmed by timestamp, not inferred.

---

## Part C — Social posting cadence

[DB-QUERY] `posted_sets` per day, last 35 days: **32 of 34 days have exactly 1 post**, gaps only on 08-19 and 09-13/09-14. This is a large, real improvement over the July baseline (~50% gap rate, gated on a strict ≥10-gallery-image threshold).

- **09-13/09-14 gap**: [GH-API] `social-automation.yml` failed both days — confirmed same root cause as Part A's storage-quota incident (`Traceback... pydantic... postgrest`, run `34756410776`).
- **08-19 gap**: [GH-API] run `32227005002` shows `conclusion: cancelled`, not a candidate-gate no-op or a crash. **UNVERIFIED** — did not dig further into why it was cancelled (manual cancel vs. a runner-level issue); flagging rather than guessing.

**New finding, not in any prior tracker entry — the `posted_sets` metric is now masking a live, ongoing Instagram failure.** [RUN-LOG] The most recent `social-automation.yml` run that still shows in the daily cadence as "1 post" (09-18, run `35340468885`, set `76210-1`) actually **failed to post to Instagram** — `Instagram API error during carousel child 1 create: [10] (#10) Application does not have permission for this action` — and only succeeded on YouTube. The pipeline still called `Marked 76210-1 as posted` and recorded a `posted_sets` row, because a partial (YouTube-only) success still counts as "posted" for this metric. **Confirmed as the same error recurring in a second, independent pipeline**: [RUN-LOG] `video-retry-missing-platform.yml`'s very first real scheduled-eligible run (`35421866301`, 2026-09-19 04:39 UTC) hit the identical `(#10) Application does not have permission for this action` retrying the same stuck row's Instagram side. This is the Meta Advanced-Access gap that ACTION 3 (Instagram App Review) exists to close — not a new bug, but its blast radius is now confirmed to span at least 2 independent pipelines (social-automation, VID-P4 retry), not 1. No existing GitHub issue names this specific recurring error class directly — **filed as issue #141** (see below).

---

## Part D — Database heartbeat

[DB-QUERY], fresh this session, project `hqpaiarhmiocmjrzjhtw`:

| table | count | max timestamp |
|---|---|---|
| store_prices | 1,881 | 2026-09-19 03:34:03 |
| price_snapshots | 135,820 | 2026-09-18 |
| raw_signals | 48,985 | 2026-09-18 19:53:41 |
| news_articles | 454 | 2026-09-18 13:07:06 |
| blog_posts | 25 | 2026-07-19 10:06:05 |
| reviews | 195 | 2026-09-18 13:07:18 |
| posted_sets | 92 | 2026-09-18 11:43:04 |
| video_posts | 67 | 2026-09-18 05:20:22 |
| quiet_panic_posts | 32 | 2026-09-18 08:41:08 |

All fresh (within 24h except `blog_posts`, which is a retired/frozen content type per existing tracker context, not a stall).

`pending_drafts` by status: `approved`=28, `draft`=149, `failed_lint`=3, `published`=641, `rejected`=323.

**`video_posts`/`quiet_panic_posts` status backlog check** (the exact gap issue #137/PRs #138–139 were built to close):
- `video_posts`: 1 row stuck at `posted_yt` (missing IG) — this is the row `video-retry-missing-platform.yml` is correctly attempting every hour, blocked purely by the `(#10)` permission error (Part C), not a code bug. Working as designed; blocked on ACTION 3, not on anything in this repo.
- `quiet_panic_posts`: 9 rows `publish_blocked`, all with `gate_override=false`, dated 07-29 through 09-09 — [DB-QUERY] all are legitimate gate rejections (never manually overridden), no fresh accumulation since 09-09 (10 days stale, consistent with normal quality-gate behavior, not a new stuck-queue).

---

## Part E — Live site sweep

[LIVE-FETCH] fresh curls against `https://bricksofindia.com` this session (representative sample, not the full 40-route sweep — Parts A/B/C/D surfaced no site-facing regression risk this cycle to justify the full sweep's cost):

| route | status |
|---|---|
| `/` | 200 |
| `/sets` | 200 |
| `/news` | 200 |
| `/deals` | 200 |
| `/lab/retiring-soon` | 200 |
| `/reviews` | 200 |
| `/guides` | 200 |
| `/opinion` | 308 → `/news` (intentional retirement redirect, matches known state) |

**SSL:** `notAfter=Dec 10 17:49:37 2026 GMT` — valid, ~3 months of runway, no action needed.

---

## Part F — Tracker drift audit

`BOI_MASTER_TRACKER.md`'s most recent entries (2026-09-18T04:35Z deploy-queue resolution, 2026-09-17T19:41Z storage-quota verification pass, 2026-09-17 storage-quota investigation) are **exceptionally current and evidence-dense** — most of what this audit independently re-derived (storage-quota cascade, catalogue-audit/technical-hygiene/code-audit's three real failures, IG token runway) was already documented there with matching or near-matching numbers. **No drift or self-contradiction found** between those entries and this audit's fresh evidence.

**One item this audit adds that the tracker doesn't yet have:** the `(#10)` permission error's confirmed second-pipeline recurrence (Part C) — logged to the tracker below, filed as issue #141.

**One item flagged, not actioned:** a Cloudflare Workers production deploy (run `35421858547`, building commit `ba8195e`, PR #139's merge) is currently sitting at `Deploy (wrangler): waiting` for approval. [REPO-GREP] Ancestry checked back to the last-approved commit `d880193`: the only 3 commits since are `9cd9550` (tracker docs only), `9eb6280` (PR #138 — Python video-pipeline scripts + new GH workflow YAML only), `ba8195e` (PR #139 — same class). `git diff --stat d880193 ba8195e -- src/ next.config.mjs package.json package-lock.json` returns **empty** — zero change to anything in the deployed Next.js/Cloudflare bundle. However, PR #138's changes touch `assert_all_gates_passed()`/`gate_override` handling, which is arguably adjacent to Tier 2 criterion 1's "video pipeline approval logic" carve-out, even though that code isn't part of what Wrangler would actually deploy. Per this session's standing instruction ("ambiguous cases default to Tier 2, chat review + Abhinav's explicit go-ahead required"), **this deploy was left untouched, not approved**, and is surfaced here for Abhinav's explicit go-ahead rather than decided unilaterally.

The much older `## Pending — Next Up (as of 2026-06-21)` section (line 2715+) was not re-audited item-by-item this pass — it predates all of the 09-17/09-18 entries above by nearly 3 months and appears superseded by the more current entries at the top of the file; a full line-by-line re-derivation of that ~90-item historical register was out of scope for this pass given the top-of-file entries already cover everything this audit's Parts A–E actually touched. Flagging this as a real gap, not silently skipped: **that section's own header timestamp is itself tracker drift** (should either be updated or explicitly marked superseded, the way the CRITICAL-01 entry at line 3503 already does for itself).

---

## Part G — Repo hygiene

- `npm audit --audit-level=high`: 9 vulnerabilities (2 moderate, 7 high) — same as Part A/code-audit.yml, tracked under PR #41.
- `TODO`/`FIXME` in `src/`: **0** [REPO-GREP].
- `git branch -a`: **33 local branches** (most named after already-merged/closed PRs — e.g. `fix/vid-p4-*`, `fix/catalogue-audit-mrp-universe` — likely safe to prune but not touched here, no destructive action taken without an explicit ask) + 29 remote branches on `origin`, several with no local counterpart (`origin/fix/backlog-124-126-128`, `origin/fix/ig-token-refresh-bom-strip`, `origin/fix/pr-2b-3.5-generator-runs-writes`, `origin/fix/retry-missing-platform-real-exit-code`, `origin/fix/groq-model-canary-qwen-decommissioned`, `origin/chore/retention-cleanup-and-manifest-fix`, `origin/feat/pr-2b-3.7-filler-filter`). Not classified individually this pass (matches prior audits' scale-driven scoping call) — flagged for a future pass if Abhinav wants a cleanup.
- **Untracked files sitting in the working tree**, found via `git status` at the start of this session, not part of this audit's own output: 19 loose `.patch` files (`0002-...patch` through `0020-...patch`) and `GEMINI_ACCESS_CHECK.md` in the repo root. Not committed, not touched, not deleted — flagged since their origin/purpose is unclear from this session alone and deleting someone's in-progress work without asking would violate this session's own safety rules.

---

## OPERATOR-CONFIRM — questions for Abhinav

1. **Deploy approval (Part F):** commit `ba8195e` (PR #139) is sitting at `Deploy (wrangler): waiting`. Ancestry-checked clean of any unapproved Tier 2 change to the deployed bundle itself, but touches video-pipeline gate logic in the underlying (non-deployed) Python scripts. Approve this Cloudflare deploy, or hold?
2. **video-retry-missing-platform.yml's cron** (Part A) hasn't fired on schedule yet as of this audit (only manual dispatches exist) — will re-check next audit; no action needed from you now, just flagging so it isn't forgotten if it's still dispatch-only a week from now.
3. **09-13 social-automation cancellation** (Part C, `run 32227005002`... correction, the cancelled run referenced is from 08-19, run `32227005002`) — was this a manual cancel on your end, or unexplained? Machine evidence alone can't distinguish the two.

---

## UNVERIFIED

- **08-19 social-automation cancellation reason** — run shows `conclusion: cancelled` with no further diagnostic captured this pass; not guessed as either "manual" or "infra."
- **video-retry-missing-platform.yml's actual unattended cron fire** — not yet observed (workflow is <1 hour old as of this audit); flagged for next audit, not asserted broken or working.
- **Full line-by-line re-audit of the pre-2026-09-17 `## Pending — Next Up` tracker register** (~90 items, dated as of 2026-06-21) — out of scope this pass given the more current top-of-file entries already supersede everything this audit's evidence gathering actually touched; a dedicated pass would be needed to either confirm or retire that section wholesale.
